<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cakeshop.domain.statistics.mapper.DailyStatisticsAggregationMapper">
    <!-- 사용자 입력은 #{}만 사용하고 ${}는 사용하지 않는다. -->

    <insert id="upsertDailyStatistics">
        INSERT INTO daily_statistics (
            statistics_date,
            total_order_count,
            completed_order_count,
            canceled_order_count,
            total_sales_amount,
            aggregated_at
        )
        VALUES (
            #{statisticsDate},
            #{source.totalOrderCount},
            #{source.completedOrderCount},
            #{source.canceledOrderCount},
            #{source.totalSalesAmount},
            CURRENT_TIMESTAMP(6)
        )
        ON DUPLICATE KEY UPDATE
            total_order_count = VALUES(total_order_count),
            completed_order_count = VALUES(completed_order_count),
            canceled_order_count = VALUES(canceled_order_count),
            total_sales_amount = VALUES(total_sales_amount),
            aggregated_at = VALUES(aggregated_at)
    </insert>

    <update id="updateDailyAdditionalMetrics">
        UPDATE daily_statistics
        SET new_member_count = #{source.newMemberCount},
            withdrawn_member_count = #{source.withdrawnMemberCount},
            new_post_count = #{source.newPostCount},
            coupon_usage_count = #{source.couponUsageCount},
            refund_amount = #{source.refundAmount},
            valid_payment_order_count = #{source.validPaymentOrderCount},
            additional_metrics_aggregated_at = CURRENT_TIMESTAMP(6)
        WHERE statistics_date = #{statisticsDate}
    </update>

    <delete id="deleteDailyProductStatistics">
        DELETE FROM daily_product_statistics
        WHERE statistics_date = #{statisticsDate}
    </delete>

    <insert id="insertDailyProductStatistics">
        INSERT INTO daily_product_statistics (
            statistics_date,
            product_id,
            product_name,
            order_count,
            sales_quantity,
            sales_amount
        )
        VALUES
        <foreach collection="sources" item="source" separator=",">
            (
                #{statisticsDate},
                #{source.productId},
                #{source.productName},
                #{source.orderCount},
                #{source.salesQuantity},
                #{source.salesAmount}
            )
        </foreach>
    </insert>

    <update id="updateProductAggregatedAt">
        UPDATE daily_statistics
        SET product_aggregated_at = CURRENT_TIMESTAMP(6)
        WHERE statistics_date = #{statisticsDate}
    </update>
</mapper>
