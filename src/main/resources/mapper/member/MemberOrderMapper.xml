<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
******************************
작성자 : 주환
담당자 : 수민
작성일 : 2026-08-11
기능 : 주문 관리자 권한 확인 SQL
설명 : 주문·결제 도메인이 members 테이블을 직접 읽지 않도록 현재 활성 ADMIN 여부만 제공한다.
******************************
-->
<mapper namespace="com.cakeshop.domain.member.mapper.MemberOrderMapper">

    <select id="existsActiveAdmin" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
              FROM members
             WHERE id = #{memberId}
               AND role = 'ADMIN'
               AND status = 'ACTIVE'
        )
    </select>
</mapper>
