<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cakeshop.domain.review.mapper.ReviewReplyMapper">
    <!-- 사용자 입력은 #{} 만 사용 (${} 금지) -->
    <!-- reviews·review_replies 외의 테이블을 JOIN 하지 않는다 (DOMAIN.md 2.7) -->

    <insert id="insertForPublishedReview" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO review_replies (review_id, admin_id, content)
        SELECT #{reviewId}, #{adminId}, #{content}
          FROM reviews
         WHERE id = #{reviewId}
           AND status = 'PUBLISHED'
    </insert>

    <update id="updateContentForPublishedReview">
        UPDATE review_replies rep
          JOIN reviews r ON r.id = rep.review_id
           SET rep.content = #{content}
         WHERE rep.review_id = #{reviewId}
           AND r.status = 'PUBLISHED'
    </update>

    <select id="findByReviewId" resultType="com.cakeshop.domain.review.dto.query.ReviewReplyRow">
        SELECT <include refid="replyColumns"/>
          FROM review_replies
         WHERE review_id = #{reviewId}
    </select>

    <select id="findByReviewIds" resultType="com.cakeshop.domain.review.dto.query.ReviewReplyRow">
        SELECT <include refid="replyColumns"/>
          FROM review_replies
         WHERE review_id IN
        <foreach collection="reviewIds" item="reviewId" open="(" separator="," close=")">
            #{reviewId}
        </foreach>
    </select>

    <sql id="replyColumns">
        id, review_id AS reviewId, content, created_at AS createdAt
    </sql>
</mapper>
