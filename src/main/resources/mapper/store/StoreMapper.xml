<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cakeshop.domain.store.mapper.StoreMapper">
    <!-- 작업 규칙: 컬럼을 명시하고 모든 사용자 입력은 #{}로 바인딩한다. -->
    <resultMap id="storeResultMap" type="com.cakeshop.domain.store.entity.Store">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="imageUrl" column="image_url"/>
        <result property="address" column="address"/>
        <result property="phone" column="phone"/>
        <result property="pickupPlace" column="pickup_place"/>
        <result property="pickupStartTime" column="pickup_start_time"/>
        <result property="pickupEndTime" column="pickup_end_time"/>
        <result property="pickupIntervalMinutes" column="pickup_interval_minutes"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <resultMap id="businessHourResultMap" type="com.cakeshop.domain.store.entity.StoreBusinessHour">
        <id property="id" column="id"/>
        <result property="storeId" column="store_id"/>
        <result property="dayOfWeek" column="day_of_week" javaType="java.time.DayOfWeek"/>
        <result property="openTime" column="open_time"/>
        <result property="closeTime" column="close_time"/>
        <result property="closed" column="is_closed"/>
    </resultMap>

    <resultMap id="holidayResultMap" type="com.cakeshop.domain.store.entity.StoreHoliday">
        <id property="id" column="id"/>
        <result property="storeId" column="store_id"/>
        <result property="holidayDate" column="holiday_date"/>
        <result property="reason" column="reason"/>
    </resultMap>

    <select id="findStoreById" resultMap="storeResultMap">
        SELECT id,
               name,
               description,
               image_url,
               address,
               phone,
               pickup_place,
               pickup_start_time,
               pickup_end_time,
               pickup_interval_minutes,
               created_at,
               updated_at
          FROM store
         WHERE id = #{storeId}
    </select>

    <select id="findBusinessHours" resultMap="businessHourResultMap">
        SELECT id, store_id, day_of_week, open_time, close_time, is_closed
          FROM store_business_hour
         WHERE store_id = #{storeId}
         ORDER BY CASE day_of_week
                    WHEN 'MONDAY' THEN 1 WHEN 'TUESDAY' THEN 2 WHEN 'WEDNESDAY' THEN 3
                    WHEN 'THURSDAY' THEN 4 WHEN 'FRIDAY' THEN 5 WHEN 'SATURDAY' THEN 6
                    WHEN 'SUNDAY' THEN 7
                  END
    </select>

    <select id="findHolidays" resultMap="holidayResultMap">
        SELECT id, store_id, holiday_date, reason
          FROM store_holiday
         WHERE store_id = #{storeId}
           AND holiday_date &gt;= CURRENT_DATE
         ORDER BY holiday_date
    </select>

    <update id="updateBasicInfo">
        UPDATE store
           SET name = #{name},
               description = #{description},
               image_url = #{imageUrl},
               address = #{address},
               phone = #{phone}
         WHERE id = #{id}
    </update>

    <!-- 조회 이후 사진이 교체됐다면 새 URL을 지우지 않도록 기존 URL까지 조건으로 확인한다. -->
    <update id="clearImageUrl">
        UPDATE store
           SET image_url = NULL
         WHERE id = #{storeId}
           AND image_url = #{expectedImageUrl}
    </update>

    <update id="updatePickupInfo">
        UPDATE store
           SET pickup_place = #{pickupPlace},
               pickup_start_time = #{pickupStartTime},
               pickup_end_time = #{pickupEndTime},
               pickup_interval_minutes = #{pickupIntervalMinutes}
         WHERE id = #{id}
    </update>

    <!-- 7개 요일 행을 최초 생성과 이후 수정에 동일하게 사용할 수 있도록 upsert한다. -->
    <insert id="upsertBusinessHour">
        INSERT INTO store_business_hour
            (store_id, day_of_week, open_time, close_time, is_closed)
        VALUES
            (#{storeId}, #{dayOfWeek}, #{openTime}, #{closeTime}, #{closed})
        ON DUPLICATE KEY UPDATE
            open_time = VALUES(open_time),
            close_time = VALUES(close_time),
            is_closed = VALUES(is_closed)
    </insert>

    <select id="existsHolidayDate" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
              FROM store_holiday
             WHERE store_id = #{storeId}
               AND holiday_date = #{holidayDate}
        )
    </select>

    <insert id="insertHoliday" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO store_holiday (store_id, holiday_date, reason)
        VALUES (#{storeId}, #{holidayDate}, #{reason})
    </insert>

    <delete id="deleteHoliday">
        DELETE FROM store_holiday
         WHERE store_id = #{storeId}
           AND id = #{holidayId}
    </delete>
</mapper>
