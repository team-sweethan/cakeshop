@AGENTS.md

# Claude Code 부트스트랩

이 파일은 Claude Code의 프로젝트 시작점이다. 공통 규칙, 라우팅 표와 작업·검증 루프의 정본은
`AGENTS.md`이며, 여기에는 그 규칙을 Claude Code에서 운영하는 방법만 둔다. `AGENTS.md`의 내용을
다시 요약하지 않는다.

## 작업 라우팅

도메인 판별과 읽을 라우터의 선택은 `AGENTS.md`의 컨텍스트 로딩 원칙과 표를 따른다. Claude Code에서는
다음만 추가로 지킨다.

- 도메인별 지침을 루트에서 미리 import하지 않는다. 하위 `CLAUDE.md`의 지연 로드에 맡기지도 말고,
  해당 도메인 작업임을 알았다면 계획 전에 그 라우터를 직접 읽어 작업 경계와 정본을 먼저 확정한다.
- `community`와 `review`가 모두 걸릴 때만 두 라우터를 읽는다. 이름이 비슷하다는 이유로 함께 로드하지
  않는다.

## 컨텍스트 예산

- 문서 전체 묶음, 관련 없는 도메인, 완료된 조각의 기록을 선제적으로 읽지 않는다.
- 긴 문서는 목차·상태 표·현재 조각을 먼저 확인하고 요청과 연결된 절만 확장한다.
- `@` import는 시작 시 함께 적재되므로 공통 불변 규칙에만 사용한다. 선택적 자료는 Skill이나 명시적
  Read로 늦게 불러온다.
- 대화에 이미 있는 긴 규칙을 다시 인용하지 말고 정본 경로와 적용한 결정만 남긴다.

## Harness Engineering 역할 분담

- `CLAUDE.md`(루트·도메인 라우터): 정본의 위치, 작업 경계와 컨텍스트 운영 방법
- Skill: 반복 가능한 기능 개발 절차와 필요할 때만 읽을 참고 자료
- Subagent: 탐색, 테스트 분석, 독립 검증처럼 격리할 수 있는 작업
- Hook: 특정 시점에 반드시 실행되어야 하는 기계적 검사와 금지 조건

현재 세션에 등록된 구성요소만 사용한다. 존재하지 않는 Skill·Subagent·Hook을 가정하지 말고 이 파일과
도메인 라우터만으로 진행한다. Hook 실패를 우회하거나 출력을 숨기지 않는다.

## 루프 운영

작업·검증 루프의 단계는 `AGENTS.md`를 그대로 따른다. Claude Code에서는 두 가지를 더한다.

- 경계가 분명한 넓은 탐색과 테스트 결과 분석은 Subagent로 분리해 주 컨텍스트를 아낀다.
- 완료 보고 전에 가능하면 구현과 분리된 관점으로 diff와 누락된 경계를 검토한다.

문서나 코드에 답이 없는 업무 결정, 다른 담당자의 기존 계약 변경, 반복 검증으로 해소되지 않는 충돌은
루프 안에서 임의로 결정하지 말고 사용자에게 구체적인 선택 지점과 근거를 알린다.
