@AGENTS.md

# Claude Code 부트스트랩

이 파일은 Claude Code의 프로젝트 시작점이다. 공통 프로젝트 규칙은 `AGENTS.md`가 유일한 진입 정본이며,
여기에는 Claude Code에서 컨텍스트와 개발 루프를 운영하는 방법만 둔다.

## 작업 라우팅

요청을 받은 직후 기능 도메인과 작업 유형을 판별한다. 도메인별 지침을 루트에서 미리 import하지 않는다.

- `community` 기능이면 계획 전에
  `src/main/java/com/cakeshop/domain/community/CLAUDE.md`를 읽고 그 라우팅을 따른다.
- `review` 후기 기능이면 계획 전에
  `src/main/java/com/cakeshop/domain/review/CLAUDE.md`를 읽고 그 라우팅을 따른다.
- 두 도메인이 모두 걸릴 때만 두 파일을 읽는다. 이름이 비슷하다는 이유로 함께 로드하지 않는다.
- 다른 도메인은 `AGENTS.md`의 컨텍스트 표에서 필요한 정본만 선택한다.

Claude Code가 하위 디렉터리의 `CLAUDE.md`를 지연 로드할 수 있더라도, 해당 도메인 작업임을 이미 알았다면
설계 전에 직접 읽어 작업 경계와 정본을 먼저 확정한다.

## 컨텍스트 예산

- 문서 전체 묶음, 관련 없는 도메인, 완료된 조각의 기록을 선제적으로 읽지 않는다.
- 긴 문서는 목차·상태 표·현재 조각을 먼저 확인하고 요청과 연결된 절만 확장한다.
- `@` import는 시작 시 함께 적재되므로 공통 불변 규칙에만 사용한다. 선택적 자료는 Skill이나 명시적 Read로
  늦게 불러온다.
- 대화에 이미 있는 긴 규칙을 다시 인용하지 말고 정본 경로와 적용한 결정만 남긴다.

## Harness Engineering 역할 분담

- `CLAUDE.md`: 항상 필요한 사실, 정본 지도, 작업 경계
- Skill: 반복 가능한 기능 개발 절차와 필요할 때만 읽을 참고 자료
- Subagent: 탐색, 테스트 분석, 독립 검증처럼 격리할 수 있는 작업
- Hook: 특정 시점에 반드시 실행되어야 하는 기계적 검사와 금지 조건

현재 세션에 관련 Skill·Subagent·Hook이 등록되어 있으면 그 역할에 맞게 사용한다. 존재하지 않는 구성요소를
사용했다고 가정하지 말고, 이 파일과 도메인 라우터의 기본 루프로 계속 진행한다. Hook 실패를 우회하거나
출력을 숨기지 않는다.

## Loop Engineering 기본 루프

1. 요청과 정본에서 수용 조건, 현재 조각, 변경 금지 범위를 확정한다.
2. 필요한 코드와 테스트만 탐색한다. 넓은 조사는 경계가 분명할 때 Subagent로 분리한다.
3. 구현과 검증 단위를 작게 계획한다.
4. 한 단위를 구현하고 가장 가까운 테스트를 실행한다.
5. 실패를 다음 입력으로 사용해 원인을 수정하고 같은 검증을 반복한다.
6. 위험에 따라 통합·전체 테스트로 넓힌다.
7. 가능하면 구현과 분리된 관점으로 diff와 누락된 경계를 검토한다.
8. 수용 조건과 필수 검사가 모두 충족될 때만 완료한다.

문서나 코드에 답이 없는 업무 결정, 다른 담당자의 기존 계약 변경, 반복 검증으로 해소되지 않는 충돌은
루프 안에서 임의로 결정하지 말고 사용자에게 구체적인 선택 지점과 근거를 알린다.
