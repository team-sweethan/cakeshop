apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # 같은 compose 네트워크 안이라 서비스 이름으로 닿는다.
    url: http://prometheus:9090
    isDefault: true
    editable: false
