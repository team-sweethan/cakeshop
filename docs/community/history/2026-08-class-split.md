# 클래스 분할 (2026-08-18)

> **끝난 트랙의 기록이다. 지금 구속하지 않는다.**
> 기능 조각이 아니라 리팩터링 트랙이라 **번호 체계가 다르다** — 여기서 말하는 `클래스 분할 조각 8`은
> 기능 조각 8(답글)과 무관하다.
>
> 패키지 4분할을 기각하고 클래스만 쪼개기로 한 판단, 그 과정에서 뒤집힌 결정 넷은
> `../decisions/decision-log-2nd.md`에 있다. 지금 코드가 어떤 모양인지는
> `../community_conventions.md`와 실제 소스가 정본이다.

| 날짜 | 내용 |
|---|---|
| 2026-08-18 | **`CommunityCommentController`·`CommunityReactionController`를 갈라 클래스 분할 10조각을 닫았다.** `CommunityController`가 449줄에서 **217줄**이 됐고, 남은 것은 목록·상세·작성·수정·삭제다. **파싱을 `CommunityRequestParams`로 모았다** — 가르면 `parsePositiveInteger`가 Controller마다 한 벌씩 늘어난다. 이미 관리자 Controller에 코드가 다른 사본이 있었고(결과는 같다), **한쪽만 고쳐지면 두 화면이 같은 주소를 다르게 해석하는데 둘 다 멀쩡해 보인다.** 관리자 쪽은 이 조각의 범위가 아니라 사본을 남겼다. **되돌아가기 검사를 `CommunityDetailRedirectTests`로 따로 세웠다** — "상세로 돌아가는 **모든** 경로가 펼친 댓글 범위를 유지한다"는 검사인데 그 경로가 이제 두 Controller에 나뉜다. Controller별로 쪼개면 그 문장이 사라지므로 둘을 함께 등록한다. **신고 재렌더링에서 `verify(never()).getPostDetail(...)` 하나를 뺐다** — 새 Controller는 게시글 Service를 아예 주입받지 않아 조회수를 올릴 방법이 **구조적으로 없다.** 생성자가 그 자리를 대신하므로 남기면 아무것도 검증하지 않는 문장이 된다(근거를 클래스 주석에 적었다). **새 Controller가 스프링에 안 잡히면 어떻게 되는지 확인했다**: `@Controller`를 떼면 5건이 깨진다 — 표준 MockMvc도 그 애노테이션이 없으면 핸들러로 등록하지 않기 때문이다. **다만 실제 컨텍스트로 이 POST 경로를 치는 검사는 하나도 없다**(`grep`으로 확인). 클래스가 스캔 범위 밖으로 나가는 경우는 여전히 조용하다 — 드물어서 하네스를 세우지 않았고, 되돌아올 계기로 여기 적어 둔다. 검증: 438 유지, 0 스킵 0 실패. `CommunityControllerTests` 53 → 34이고 옮긴 19가 새 세 클래스에 있다(53 = 34 + 5 + 5 + 9) |
| 2026-08-18 | **상세 화면 Model 조립을 `CommunityDetailModelAssembler`로 뗐고, 옮기다 검사 구멍 넷을 찾아 메웠다**(클래스 분할 조각 9). 상세 화면은 진입점이 셋이다(상세 조회, 댓글 검증 실패, 신고 검증 실패). 조립이 흩어지면 **한 경로에만 속성이 빠져도 그 화면에서만 버튼이 사라진다.** 화면 이름은 옮기지 않았다 — 어느 화면으로 갈지는 Controller의 몫이라 조립기는 `void`이고 `prepareDetail`이 두 줄로 남았다. **구멍은 탐침으로 찾았다**: `canReport`를 항상 `false`로 바꿔도 385개가 전부 통과했다. 속성별로 세어 보니 `viewerId`·`canEdit`·`canReport`·`alreadyReported` 넷에 단언이 하나도 없었다. `canComment`·`canLike`·`likedByViewer`는 `CommunityControllerTests`가 요청 경로로 보고 있어 대비가 된다. **화면 렌더링 테스트가 비어 있는 것은 정책이다** — `docs/testing.md` 8절이 "권한별 버튼·안내 문구·HTML 조각은 여기서 고정하지 않는다"로 정했고, 그래서 이 넷은 **어느 계층에서도 안 물리고 있었다.** 새 `CommunityDetailModelAssemblerTests`(12)가 그 넷만 맡는다 — 이미 보고 있는 셋을 다시 보지 않는 것은 `docs/testing.md` 4절 그대로다. **같은 탐침을 다시 걸어 이제 잡히는 것을 확인했다.** 이 넷은 틀려도 서버에 오류가 없고 버튼이 사라지거나 남을 뿐이라, 자기 계정으로 보는 개발자 눈에는 특히 안 드러난다. **정책 클래스를 화면 쪽에서 쓰지 못했다**: `canEdit`의 `viewerId != null && viewerId.equals(post.memberId())`는 `CommunityPostAccessPolicy.isAuthor`와 같은 식인데, 그 클래스가 `service` 패키지 전용이라 `controller`에서 안 보인다. 지금은 두 벌이 같은 답을 내지만 갈리면 **버튼과 실제 권한이 어긋난다**(눌러야 거절을 안다). 공개 범위를 넓힐지, View가 그 판단을 갖게 할지는 조각 10에서 정한다. **구멍을 메우면서 같은 함정을 한 번 더 밟았다**(Codex P2): `viewerId`를 비로그인 경우로만 단언했는데, **속성을 아예 안 실어도 `getAttribute`는 똑같이 `null`을 돌려준다.** 즉 그 검사는 바인딩을 하나도 지키지 않았다 — 이 문서가 여러 번 적은 "빈 곳은 실패가 아니라 통과의 모습으로 나타난다"가 검사 자체에서 재현된 것이다. 잃는 것도 조용하다: `detail.html`이 `viewerId == comment.memberId`로 **자기 댓글 삭제 버튼**을 가리므로, 바인딩이 사라지면 로그인 사용자 전원이 그 버튼을 잃는데 서버에는 오류가 없다. 로그인 조회자의 실제 ID를 단언하는 검사를 더했고, 바인딩을 지우는 탐침으로 **새 검사만 깨지고 옛 검사는 그대로 통과하는 것**을 확인했다. 검증: 425 → 438(새 검사 13), 0 스킵 0 실패 |
| 2026-08-18 | **`CommunityService`를 `CommunityPostService`로, `CommunityServiceTests`를 `CommunityPostServiceTests`로 개명했다**(클래스 분할 조각 8의 나머지 절반). 댓글·좋아요·신고가 떠난 뒤 남은 것이 게시글뿐이라 이름이 내용과 어긋나 있었다. 파일은 `git mv`로 옮겨 이력을 보존했다. **개명하면서 조용히 틀리는 자리를 하나 밟았다** — `sed`의 `\b`(낱말 경계)로 치환하니 `CommunityServiceTests`는 `Service` 뒤에 `T`가 붙어 경계가 없어 **하나도 바뀌지 않았다.** 그런데 테스트 클래스는 package-private이라 자바가 파일명과 클래스명의 불일치를 막지 않고, **컴파일도 테스트도 전부 초록이었다.** 결과 XML의 클래스 이름으로 확인해 잡았다([[korean-text-tooling-traps]]와 같은 종류다). **문서는 현재 상태를 말하는 것만 고쳤다.** `../specs/community-popular.md` H27·H38과 `../specs/community-post.md` H2b·H2d의 소유자 참조, 테스트 클래스 주석 둘이 대상이다. **반대로 완료 기록은 옛 이름 그대로 둔다** — 옛 `../PLAN.md`의 조각 절들과 `../DOMAIN.md`의 착수 시점 뼈대는 "그때 그 이름의 클래스를 추가했다"는 과거 사실이고, 개명이 그 문장을 거짓으로 만들지 않는다. 고치면 오히려 당시 사실이 왜곡된다(2026-08-02 항목의 "기록은 지우지 않는다"와 같은 판단이다). **옛 이름을 만나면 이 파일의 이 행이 현재 이름을 가리킨다.** 검증: 기준선 425, 개명 후 425로 같고 0 스킵 0 실패다 |
| 2026-08-18 | **좋아요·신고를 `CommunityReactionService`로 뗐다. 계획의 조각 8을 둘로 갈라 개명은 다음 PR로 미룬다** — `CommunityService` 참조가 15파일 44곳이라 기계적 개명과 로직 추출이 한 diff에 섞이면 리뷰가 무너지고, `docs/pull-request.md`의 "PR 하나에 목적 하나"에도 어긋난다. **신고와 좋아요가 글을 읽는 방법이 다른 것이 이 조각의 핵심이다**: 신고는 `communityService.getVisiblePost`를 거치지만 **좋아요만 `lockPost`로 직접 읽는다.** 뒤따르는 재계산이 같은 `posts` 행에 배타 잠금을 걸어야 하고 그 잠금을 먼저 잡지 않으면 H13과 같은 모양의 교착이 된다(H15·H17로 두 번 더 겪은 자리다). 읽는 방법만 다르고 판단하는 규칙은 조각 6의 `CommunityPostAccessPolicy` 하나로 같다 — 그래서 새 Service에 그 이유를 주석으로 남겼다. **잠금 방어선이 실제로 물리는지 일부러 고장 내어 확인했다**: `addLike`에서 잠금을 쓰기 뒤로 미루면 8건이 깨지고, 그중 셋이 **실제 MariaDB 동시성 테스트**다(`addLike_concurrentLikesByDifferentMembers_countsEachWithoutDeadlock`가 정확히 교착 방어선이다). 단위 `InOrder` 검사만이 아니라 진짜 동시 요청으로도 잡힌다는 뜻이다. **테스트도 함께 갈랐다** — 9메서드를 `CommunityReactionServiceTests`로 옮겨 `CommunityServiceTests`가 32→23메서드, 706→561줄이 되어 `docs/testing.md` 204의 30개 기준 아래로 내려왔다. 총 개수는 그대로다(기준선 425, 작업 후 425. `CommunityServiceTests` 44→30, 새 클래스 14로 44 = 30 + 14). **`../specs/community-reaction.md` H15·H16의 소유자 참조를 같은 변경에서 갱신했다** — 직전 조각에서 Codex가 잡았던 자리라 이번에는 착수 시점에 함께 처리했다 |
| 2026-08-18 | **댓글을 `CommunityCommentService`로 뗐고, 테스트도 함께 갈랐다.** `getComments`·`addComment`·`deleteComment`와 딸린 비공개 검증 둘이 옮겨 갔다. **댓글을 달 수 있는 글인지는 게시글 쪽이 계속 판단한다** — `getCommentablePost`를 `CommunityService`에 남기고 댓글 Service가 그것을 부른다. 여기서 글을 다시 읽으면 노출 규칙이 두 벌이 되고, 그 두 벌은 관리자가 글을 차단하는 순간 갈린다(조각 6이 방금 없앤 자리다). **순환 참조는 없다** — `CommunityService`를 부르는 Service가 하나도 없고 컨트롤러만 쓴다는 것을 확인했고, 도메인 안 Service 간 참조는 `CommunityHomeQueryService`→`CommunityNoticeService`의 선례가 있다. **관리자 컨트롤러는 게시글 Service 의존이 통째로 사라졌다** — `getComments` 하나만 쓰고 있어서 필드가 교체됐다. **테스트를 함께 가른 것이 이 조각의 핵심이다**: 분할의 근거 자체가 `docs/testing.md` 204의 "30개·600줄" 신호였는데(`CommunityServiceTests` 898줄 42메서드), 운영 클래스만 갈라 놓고 테스트를 두면 그 근거가 그대로 남는다. 댓글 검사 10메서드를 `CommunityCommentServiceTests`로 옮겨 42→32메서드가 됐다. **`form_trims...`만 남겼다** — 그 검사의 요지가 "게시글과 댓글 입력값이 같은 규칙으로 정리된다"라 나란히 두지 않으면 뜻이 사라진다. 그래서 `commentFormOf` 헬퍼 넷 줄만 양쪽에 둔다. **댓글 Service 테스트에서 게시글 Service를 목으로 갈지 않았다** — 목이면 차단 글 표가 아무것도 검증하지 않게 된다(조각 5에서 로더를 실제로 감싼 것과 같은 판단이다). **옮기다 흘린 검사가 없는지 총 개수로 확인했다**: `dev` 기준선 425, 작업 후 425. `CommunityServiceTests`가 61에서 44로 줄고 새 클래스가 17이다(61 = 44 + 17). **덧붙여 조각 6 PR(#282)에 적은 "424 테스트"는 틀린 수치였다** — 같은 명령의 실제 기준선은 425다. **하네스 소유자를 가리키는 정본 참조 둘을 함께 갱신했다**(Codex P2): `../specs/community-comment.md` H11과 `CommunityScreenRenderingTests`의 클래스 주석이 각각 0행 재확인과 댓글 상한 계산을 여전히 `CommunityServiceTests`로 안내하고 있었다. **검사를 옮기면 그 검사를 가리키는 문장이 조용히 거짓이 되고, 다음 사람은 정본을 따라가서 없는 검사를 찾는다.** 처음에 놓친 이유는 결정 기록만 갱신했기 때문이다 — 도메인 라우터가 "대상은 내가 고친 문장이 아니라 이 세션에서 읽은 문서 전부"라고 적어 둔 자리가 정확히 여기다. 남은 `CommunityServiceTests` 참조 일곱(인기글·게시글·좋아요·신고)은 옮기지 않은 검사라 그대로 두었고, 옮긴 열 메서드의 이름이 문서에 남아 있지 않은 것도 확인했다 |
| 2026-08-18 | **게시글 접근 규칙을 `CommunityPostAccessPolicy`로 뗐고, 계획했던 범위보다 좁게 잘랐다.** 클래스 분할 계획은 `requireVisiblePost`·`requireEditablePost`·`toDetailView`·`isVisibleTo`·`isAuthor` 다섯을 옮기기로 했지만 **그 다섯은 한 덩어리가 아니었다.** 앞의 둘은 매퍼를 읽고, `toDetailView`는 회원을 조회한다 — 다 옮기면 매퍼와 회원 로더를 함께 들고 화면 DTO까지 만드는 클래스가 되어 규칙이 아니라 **두 번째 Service**가 되고, `toDetailView`는 조각 9(`CommunityDetailModelAssembler`)가 가져갈 자리라 그쪽과도 부딪친다. 그래서 **협력자가 하나도 없는 순수 규칙만** 뗐다(`isAuthor`·`isVisibleTo`·`requireVisible`·`requirePublished`·`requireEditable`). 매퍼 읽기와 `toDetailView`는 `CommunityService`에 남는다. **대신 계획에 없던 중복을 하나 없앴다** — `requireLikeablePost`가 같은 노출 규칙을 `PostLockRow`에 대고 **인라인으로 한 벌 더** 갖고 있었다(`DELETED \|\| (BLOCKED && !작성자)`). 두 행 타입이 `(status, memberId)`로 같아서 규칙을 `PostStatus` 하나로 받게 하니 좋아요·댓글 두 경로가 같은 문장을 쓴다. 이 문서가 여러 번 적은 "두 벌이 되는 순간 갈린다"가 이미 일어나 있던 자리다. **잠금은 건드리지 않았다** — `lockPost` 호출 위치와 순서가 그대로여서 H13·H15·H17과 R14의 판단이 유지된다. **일부러 고장 내어 검사가 무는지 확인했다**: `requirePublished`를 무력화하면 5건(댓글·좋아요·수정·삭제 재확인 경로 전부), `BLOCKED → isAuthor`를 `false`로 바꾸면 6건이 깨진다 — 그중 `getPostDetail_blockedPost_author_returnsPostWithBlockedReason`과 `communityDetail_blockedPostAuthor_showsReasonAndHidesActions`가 **"차단된 글은 작성자에게만 보이고 그래도 댓글은 못 단다"는 비대칭**을 정확히 문다. 33클래스 424테스트 0스킵 0실패. **주의: 기능 조각 6(조회수 중복 방지, 2026-08-04 완료 — `../PLAN.md` 조각 순서 표)과 번호만 같고 다른 것이다** — 클래스 분할 조각 번호는 이 파일 안에서만 쓰는 번호이고, **10조각 계획의 전체 순서는 어디에도 적히지 않았다.** 여기 남은 행이 실제로 끝난 것들이다 |
| 2026-08-18 | **두 벌이던 회원 배치 조회를 `CommunityMemberViewLoader` 하나로 합쳤다.** `CommunityService.findAuthors`와 `CommunityAdminService.findMembers`가 파라미터 타입만 다르고 본문이 같았다(`null` 거르기 → `distinct` → `getMembersByIds` → id로 Map). `docs/decisions/domain-oriented-architecture.md`가 "경계를 잘못 나누면 비슷한 조립 코드가 여러 곳에 생긴다"고 적어 둔 그 모양이다. **이름을 `AuthorLoader`가 아니라 `MemberViewLoader`로 한 이유**: 관리자 쪽은 작성자만이 아니라 **차단 관리자(`blockedBy`)와 신고자(`reporterId`)**도 같은 경로로 싣는다. `Author`로 지으면 다음 사람이 신고자 조회를 여기 두지 않고 새로 만든다. **파라미터를 `Stream<Long>`으로 둔 것은 관리자 상세 때문이다** — 2026-08-05 조각 10c가 차단 관리자의 `LEFT JOIN`을 걷어내면서 작성자와 차단 관리자 **두 ID를 한 번의 배치 조회로 함께** 받도록 정했고, 그 자리가 `Stream.of(post.memberId(), post.blockedBy())`다. `List`를 넘기던 쪽은 `.stream()`만 붙이면 된다. **두 서비스는 `MemberCommunityQueryService` 대신 로더를 주입받는다** — 생성자 인자 수는 그대로이고, **회원 계약을 아는 커뮤니티 파일이 둘에서 하나로 줄었다.** `CommunityDomainBoundaryTests`가 고정한 허용 계약 둘은 그대로 참조되므로 검사도 그대로 통과한다. **단위 테스트에서 로더를 목으로 갈지 않고 실제 로더로 감쌌다** — 목으로 바꾸면 `distinct`와 `null` 거르기가 아무 검사도 받지 않게 되고, 그것이 정확히 N+1을 막는 부분이다. 기존 `getMembersByIds` 스텁도 그대로 산다. 슬라이스 테스트 셋(`CommunityMemberContractTests`·`CommunityQueryCountTests`·`CommunityViewCountTests`)의 `@Import`에 로더를 더했다 — 협력자가 늘 때마다 이 목록을 본다는 조각 7c의 교훈 그대로다. **옮기면서 두 메서드의 javadoc은 가져오지 않았다**(무주석 원칙). N+1 근거는 H1b와 `CommunityQueryCountTests`가 물고, 탈퇴 회원 처리는 각 View의 `of`·`withAuthor`가 갖는다 |
| 2026-08-18 | **죽은 코드 둘을 지우고 구조 하네스 둘을 고쳤다**(위 결정의 선행 정리). 삭제: `CommunityApiController`(`@RestController`인데 매핑 0개, 본문이 `// TODO` 한 줄, 저장소 전체 참조 0건)와 `CommunityAdminMapper.countPendingReports`(운영 호출부 없음. 관리자 상세의 미처리 건수는 `reports.stream().filter(ReportView::isPending).count()`가 센다). 유일한 참조였던 `CommunityMapperTests`의 단언도 함께 뗐다 — **바로 다음 줄의 `findReportsByPost` 단언이 `containsExactlyInAnyOrder(REJECTED, RESOLVED)`로 "PENDING이 하나도 없다"를 이미 더 강하게 고정한다.** 하네스: `CommunityDtoBoundaryTests.javaFiles`에 `isNotEmpty`를 넣고 양쪽 `Files.list`를 `Files.walk`로 바꿨다. **고치기 전에는 dto 밑에 폴더가 하나만 생겨도 목록이 비고 `noneMatch`·`doesNotContain`이 빈 리스트 위에서 공허하게 통과했다** — 실패가 아니라 무검사이고, 이 문서가 두 번 적은 "빈 곳은 실패가 아니라 통과의 모습으로 나타난다"와 같은 모양이다. 존재하지만 `.java`가 없는 디렉터리를 물려 새 단언이 실제로 무는 것을 확인했다. **매퍼 XML은 `resources/mapper/community` 1단 깊이를 유지해야 한다.** 처음에는 이 제약을 `Files.list`가 **우연히** 막고 있었다 — 하위 폴더로 내리면 목록이 비어 `isNotEmpty`가 실패하는 방식이다. 그런데 같은 커밋에서 `Files.walk`로 바꾸면서 그 우연이 사라졌고, **결정 로그에는 여전히 "하위 폴더로 내리면 깨진다"가 적혀 있었다**(PR #276 Codex P2). 지적이 맞았다 — 실제로 XML 하나를 하위 폴더에 넣고 돌려 보니 그대로 통과했다. 되돌리지 않고 **위치 제약을 따로 세우는 쪽**을 택했다(`communityMapperXml_staysDirectlyUnderMapperDirectory`): 회원 경계 검사는 파일이 어디 있든 물어야 하므로 `walk`가 맞고, 위치는 위치대로 대놓고 검사하는 것이 맞다. **`Files.list`로 되돌리는 안은 버렸다** — 그 방식은 실패 메시지가 `커뮤니티 매퍼 XML이 있어야 한다`라서 진짜 문제(폴더를 팠다)를 가리키지 못하고, 무엇보다 **우연히 지켜지던 것을 계속 우연에 맡기는 선택**이다. 이 문서가 두 번 적은 함정과 같은 자리다 |
