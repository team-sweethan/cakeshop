# cakeshop 팀 미결정 항목

> 팀이 **아직 정하지 않은 것만** 남긴 문서다. 확정된 규칙은 각 정본 문서에 있으며 여기서 반복하지 않는다.
> 표기: ✅ 결정됨 · 🟡 제안(검토 필요) · ⬜ 미정

| 주제 | 정본 |
|---|---|
| 도메인 담당과 협업 경계 | [`AGENTS.md`](../AGENTS.md) |
| 코드 구조·스타일·DB 규약·Flyway migration | [`conventions.md`](conventions.md) |
| 테스트 작성 규칙 | [`testing.md`](testing.md) |
| PR·커밋·리뷰 요청과 병합 기준 | [`pull-request.md`](pull-request.md) |
| 상태값 설계와 도메인별 status 인벤토리 | [`status-design.md`](status-design.md) |
| 로컬 환경 준비와 DB 초기화 | [`README.md`](../README.md) |
| 화면 템플릿 규격 | [`frontend-template-format.md`](frontend-template-format.md) |

여기 있는 항목이 정해지면 해당 정본 문서로 옮기고 이 문서에서 지운다.

공용 RDS 운영(반영 담당자·이력 기록·롤백 절차)은 후반으로 미뤘다. `rds` 프로필은 Flyway를 실행하지 않으므로, 착수 시점에 적용 절차부터 정한다. 규칙 자체는 [`conventions.md`](conventions.md) 6-1절에 있다.

---

## 1. 도메인 간 연결

담당 도메인 표와 오너십 규칙은 [`AGENTS.md`](../AGENTS.md)「도메인 담당과 협업 경계」가 정본이다. 아래는 **도메인이 서로 무엇을 주고받는지** 합의할 목록이다.

| 연결 | 합의할 기능과 데이터 | 담당자 |
|---|---|---|
| 장바구니 → 상품 | 상품·옵션·현재 가격 확인 | |
| 주문 → 회원·장바구니·상품 | 주문자, 주문 대상, 재고 확인 | |
| 결제 → 주문 | 결제 가능 여부와 주문 상태 변경 | |
| 쿠폰 → 주문 | 할인 계산, 사용 및 복구 | |
| 리뷰 → 주문 | 구매·픽업 완료 확인 | |
| 알림 → 주문·결제 | 이벤트별 알림 생성 | |
| 채팅 → 회원·주문 | 상담 대상과 주문 정보 조회 | |

- `home`: 전용 Mapper 없이 각 도메인 공개 View를 단방향 조합하는 얇은 계층 — 이미 매장 조회에 연결됨. 별도 개발 대상이 아니다. 각 도메인이 완성되면 자기 공개 View를 home에 물려주기만 하면 된다.
- `statistics`: 타 도메인 데이터에 의존하며 현재 목업 상태 → 각 도메인 완성 후 후반에 별도 착수(담당 추후 결정).

> ⚠️ **결제 흐름 3인 공유**: `cart`(수민) → `order`·`payment`(주환) ← `coupon`(정후)이 `OrderStatus` 상태 전이와 checkout 흐름을 공유한다. 착수 초반에 셋이 인터페이스(장바구니→주문 생성 · 쿠폰 적용 시점 · 결제 상태 콜백)를 먼저 맞추고 각자 브랜치를 판다. 충돌·재작업이 가장 많이 나는 지점이다.

### 원칙

- FK 또는 공통 인터페이스 변경은 관련 담당자 모두가 검토한다.
- 다른 도메인은 Mapper가 아니라 합의된 Service 인터페이스를 통해 사용한다.
- 여러 도메인이 참여하는 작업은 트랜잭션을 소유할 도메인을 먼저 정한다.

---

## 2. 핵심 비즈니스 규칙

**이 문서에서 가장 중요한 절이다.** 여기가 비어 있으면 담당자마다 다르게 구현하고 통합 시점에 드러난다. 상태값의 *표기와 저장 방식*은 [`status-design.md`](status-design.md)가 정본이고, 아래는 *언제 누가 바꾸는가*다.

| 결정할 사항 | 최종 결정 | 관련 담당자 |
|---|---|---|
| 같은 상품·옵션을 장바구니에 다시 담을 때 처리 | | |
| 장바구니 가격과 현재 가격이 다를 때 처리 | | |
| 주문 시 상품명·옵션명·가격 스냅샷 저장 여부 | | |
| 상품 유형별 준비 기간 | `GENERAL`은 미리 제작된 재고 상품으로 보고 `preparation_days`를 0일로 고정하며 당일부터 픽업할 수 있다. `CUSTOM`은 관리자가 1일 이상으로 설정하고, 가장 빠른 픽업일은 관리자 승인일에 준비 기간을 더해 계산한다. 매장 영업일·영업시간 충족 여부는 별도로 확인한다. | 시은·주환 |
| 주문 유형별 취소 가능 조건 | `GENERAL` 주문은 픽업 시각 전까지 취소할 수 있다. `CUSTOM` 주문은 관리자 승인 전까지만 취소할 수 있으며, 승인 시 취소를 차단하고 제작을 시작한다. 취소 가능 여부는 상품의 일수 설정이 아니라 주문 유형·상태·픽업 시각으로 판단한다. `order_items.cancellation_limit_days`의 유지·사용 여부는 주문 도메인에서 결정한다. | 시은·주환 |
| 당일 픽업 상품 분류 | 별도의 당일 픽업 상품군·검색 필터·메뉴·배지를 운영하지 않는다. `GENERAL` 상품은 판매 중이고 주문 수량만큼 재고가 있으면 준비 기간 0일의 결과로 당일 픽업할 수 있다. | 시은·주환 |
| 주문 시 상품 준비 기간 스냅샷 | 주문 생성 시 상품의 `preparation_days`를 `order_items.preparation_days`에 저장한다. 이후 상품의 준비 기간이 변경되어도 기존 주문의 값과 픽업 가능일은 변경하지 않는다. | 시은·주환 |
| 재고 차감 및 복구 시점 | | |
| 관리자 승인 전후 주문 수정·취소 가능 여부 | | |
| 결제 성공과 주문 상태 변경 순서 | | |
| 주문과 결제 상태의 동기화 순서 | | |
| 결제 성공 후 DB 처리 실패 시 복구 방법 | | |
| 전체 취소와 부분 취소 지원 범위 | | |
| 취소·실패 시 복구할 데이터 | | |
| 쿠폰 사용 및 복구 시점 | | |
| 리뷰 작성 가능 조건과 작성 가능 횟수 | | |
| 채팅방 생성·종료 시점 | | |
| 알림 생성 범위와 읽음 처리 방식 | | |
| 각 상태를 변경할 수 있는 사용자 또는 시스템 | | |
| 상태 변경 시 함께 기록할 시간 정보 | | |
| 상태 규칙 변경 검토자 | | |

---

## 3. 파일 및 이미지 저장

| 결정할 사항 | 최종 결정 | 담당자 |
|---|---|---|
| 개발 및 운영 파일 저장 위치 | 🟡 프로젝트 밖 로컬 디렉토리(`.env`의 `FILE_UPLOAD_DIR`, 기본 `<user home>/cakeshop-uploads`). `LocalFileStorageClient`가 저장하고 `/uploads/**`로 서빙 | |
| DB에 저장할 경로 형식 | 🟡 웹 접근 경로(`url-prefix` 접두)를 저장 | |
| 허용 확장자와 MIME 타입 | ⬜ | |
| 최대 파일 크기 | ⬜ (현재 `application.yml`은 파일 5MB·요청 10MB) | |
| 파일명 생성 규칙 | ⬜ | |
| 데이터 삭제 시 실제 파일 처리 | ⬜ | |
